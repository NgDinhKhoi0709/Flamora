import { cn } from "@/lib/utils";

export const FlamoraLogo = ({ className }: { className?: string }) => (
    <div className={cn("font-headline text-2xl font-medium tracking-wide", className)}>
      FLAMORA
    </div>
  );
  